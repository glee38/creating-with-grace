  <% if post.errors.any? %>
    <div class="errors">
      <label><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</label>

      <ul class>
      <% post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
